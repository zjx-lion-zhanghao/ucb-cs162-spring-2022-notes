The operating system does not have a universally accepted definition.

The Internet is a large distributed system with many computer components.

The idea of an operating system is related to the historical role of computer operators and the analogy of a switchboard operator.

What is a load balancer?

Are registers, processors, memory,cache and tasks the same thing? What are their definitions?

What are the definitions of memory, storage, and segment?


attached pages
Scale the system
teaching assistants
executes instructions
store data into memory
scratch paper
RAM is usually volatile
Storage means persistent devices
Simple analogy
global/static variables
Heap segment      → dynamically allocated memory, such as malloc
Stack segment     → function calls and local variables
Here p points to nothing valid. The program tries to write to invalid memory. The OS stops it and reports:

Segmentation fault